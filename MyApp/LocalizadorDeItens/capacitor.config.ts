import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.leandro.localizadordeitens',
  appName: 'LocalizadorDeItens',
  webDir: 'dist'
};

export default config;
